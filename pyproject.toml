[build-system]
requires = ["setuptools>=67.0"]
build-backend = "setuptools.build_meta"

[tool.isort]
line_length = 120
multi_line_output = 1
include_trailing_comma = true

[tool.black]
line_length = 120
exclude = "migrations/"
